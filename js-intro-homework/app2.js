// a.
const pi = 4.14
let r = 23

area = pi * r * r
console.log(`the area of the cyrcle is ${area}`)

// b.
let c = 100;

// d.
r = c / 2*pi

// e.
D = 2*r

// f.
console.log(`The circle has a circumference of 
${c}m. Its radius is ${r} and the diameter is ${D}.`)